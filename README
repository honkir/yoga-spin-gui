
Yoga Spin GUI
=============


Building debian package:

`# dpkg-buildpackage -rfakeroot`

Adding changelog entry using `dch` from package devscripts.

`# dch -v X.Y.Z "message"`
`# dch --release`



